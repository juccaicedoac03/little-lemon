const Login = (props) => {
    return (
        <div className={props.className}>
            <div id='granMessageContainer'>
                <div id='messageContainer'>
                    <h1>Oops!...</h1>
                    <p>This feature is not available right now. Try later.</p>
                </div>
            </div>
        </div>
    );
}

export default Login;